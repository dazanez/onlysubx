enum Frecuency { daily, weekly, biweekly, monthly, custom }

enum Currency { usd, eur, cop, mxn }

enum Status { active, suspended, canceled, expired }

enum Language {local, spanish, english}

enum Role {user, admin}

enum PaymentMethodTypes { cash, debit, credit, other }
